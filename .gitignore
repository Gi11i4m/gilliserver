.DS_Store

# Secrets. This repo is public: the real versions of these live in
# /boot/gilliserver.env on the device and nowhere else. See AGENTS.md.
*.env
!*.env.example
gilliserver.env
secrets*
*.pem
*.key
id_rsa*
id_ed25519*

# WiFi credentials go straight onto the SD card, never through here.
boot/dietpi-wifi.txt

*.local
